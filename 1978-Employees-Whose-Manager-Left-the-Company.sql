# Write your MySQL query statement below
SELECT e.employee_id FROM Employees e LEFT JOIN Employees f ON e.manager_id = f.employee_id WHERE e.salary < 30000 AND f.employee_id IS NULL AND e.manager_id IS NOT NULL ORDER BY e.employee_id;